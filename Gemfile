source "http://rubygems.org"

gemspec

# gem "rails", ">= 3.0.5"

group :test do
  if RUBY_VERSION > "1.9.0"
    # gem 'simplecov', '>= 0.4.0', :require => false
  else
    # gem "rcov"
    gem "ruby-debug"
  end
end
