source "http://rubygems.org"

# Specify your gem's dependencies in mcutter.gemspec
gemspec

# gem "rails", ">= 3.0.5"
#
if RUBY_PLATFORM =~ /darwin/
  group :test do
    gem "autotest-growl"
  end
end
